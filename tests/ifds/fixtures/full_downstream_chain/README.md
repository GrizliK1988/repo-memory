# Full downstream chain

Reviewed stage-0 fixture: the changed selected value must continue through the
intermediate calculation to the return and record the execution exit. Reporting
only the first consumer is incomplete.
