# Overwrite

Reviewed stage-0 fixture: the inserted assignment becomes the last writer reaching
the return. The initial write remains code but must not be reported as a newly added
flow to the return.
